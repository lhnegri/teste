CREATE TABLE END_TIPO (
    CODIGO      INTEGER NOT NULL,
    TIPO        VARCHAR(1),
    NOME        VARCHAR(30),
    ABREVIACAO  VARCHAR(10)
);

ALTER TABLE END_TIPO ADD CONSTRAINT PK_END_TIPO PRIMARY KEY (CODIGO);