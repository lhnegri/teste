CREATE TABLE END_BAIRRO (
    CODIGO          INTEGER NOT NULL,
    NOME_EXTENSO    VARCHAR(60),
    NOME_ABREVIADO  VARCHAR(30),
    CODIGO_CIDADE   INTEGER NOT NULL,
    UF              VARCHAR(2)
);

ALTER TABLE END_BAIRRO ADD CONSTRAINT PK_END_BAIRRO PRIMARY KEY (CODIGO, CODIGO_CIDADE);
