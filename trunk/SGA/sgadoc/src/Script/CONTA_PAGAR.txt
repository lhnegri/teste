CREATE TABLE CONTA_PAGAR (
    ID               INTEGER NOT NULL,
    NOTA_FISCAL      INTEGER NOT NULL,
    NUMERO_PARCELA   VARCHAR(10) NOT NULL,
    VALOR_PAGAMENTO  DECIMAL(15,2) NOT NULL,
    VALOR_PAGO       DECIMAL(15,2),
    DATA_VENCIMENTO  DATE NOT NULL,
    DATA_PAGAMENTO   DATE,
    DATA_CADASTRO    DATE NOT NULL,
    DESCONTO_JURO    DECIMAL(15,2),
    ID_FORNECEDOR    INTEGER NOT NULL,
    OBSERVACOES      VARCHAR(255)
);

ALTER TABLE CONTA_PAGAR ADD CONSTRAINT PK_CONTA_PAGAR PRIMARY KEY (ID);

ALTER TABLE CONTA_PAGAR ADD CONSTRAINT FK_CONTA_PAGAR_1 FOREIGN KEY (ID_FORNECEDOR) REFERENCES PESSOA_JURIDICA (ID);