CREATE TABLE END_CIDADE (
    CODIGO              INTEGER NOT NULL,
    NOME                VARCHAR(60),
    CEP                 VARCHAR(8),
    UF                  VARCHAR(2),
    TIPO                VARCHAR(1),
    SIT                 VARCHAR(1),
    CODIGO_SUBORDINADO  INTEGER,
    CODIGO_UF           INTEGER
);

ALTER TABLE END_CIDADE ADD CONSTRAINT PK_END_CIDADE PRIMARY KEY (CODIGO);

ALTER TABLE END_CIDADE ADD CONSTRAINT FK_END_CIDADE_UF FOREIGN KEY (CODIGO_UF) REFERENCES END_UF (CODIGO);

