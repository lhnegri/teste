CREATE TABLE VENDA (
    ID                  INTEGER NOT NULL,
    DATA_VENDA          DATE NOT NULL,
    VALOR_TOTAL_VENDA   DECIMAL(15,2) NOT NULL,
    HORA_VENDA          TIME NOT NULL,
    CONDICAO_PAGAMENTO  VARCHAR(100) NOT NULL,
    ID_VENDEDOR         INTEGER NOT NULL,
    ID_CLIENTE          INTEGER NOT NULL,
    DESCONTO            DECIMAL(15,2),
    OBSERVACAO          VARCHAR(500),
    STATUS              VARCHAR(10) NOT NULL
);

ALTER TABLE VENDA ADD CONSTRAINT PK_VENDA PRIMARY KEY (ID);

ALTER TABLE VENDA ADD CONSTRAINT FK_VENDA_1 FOREIGN KEY (ID_VENDEDOR) REFERENCES COLABORADOR (ID) ON UPDATE CASCADE;
ALTER TABLE VENDA ADD CONSTRAINT FK_VENDA_2 FOREIGN KEY (ID_CLIENTE) REFERENCES PESSOA (ID) ON UPDATE CASCADE;

